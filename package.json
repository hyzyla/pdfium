{
  "name": "@hyzyla/pdfium",
  "version": "1.0.0",
  "description": "",
  "type": "commonjs",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -r src/vendor dist",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sharp": "^0.32.1"
  },
  "files": [
    "dist/**/*"
  ], 
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@types/node": "^20.2.5",
    "ts-node": "^10.9.1"
  }
}
